# Origins allowed to call the API, comma separated.
# Defaults to the local Vite dev server if unset.
CORS_ORIGINS=http://localhost:5173

# Step delays in seconds. The case specifies 3 and 5; lower them only to
# poke at the API by hand.
VALIDATION_DELAY=3
CALCULATION_DELAY=5
