server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback so react-router routes (e.g. /requests/:id) resolve on reload.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
