# Loaded automatically by `npm run dev` (Vite dev mode).
# Keeps local development pointed at a backend running on your machine.
# Override locally with .env.local if your backend is elsewhere.
VITE_API_URL=http://localhost:8000
