# Optional local override. `npm run dev` already points at http://localhost:8000
# via the committed .env.development, so you only need this file to point the
# frontend somewhere else. Copy it to .env.local and edit.
VITE_API_URL=http://localhost:8000
